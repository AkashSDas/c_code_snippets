#include <stdio.h>

int main(void) {
  
  // **** Pointers **** //
   
   
   // ## Introduction ## //
   
   // Pointer is a special variable that is capable of storing some address
   // It points to a memory where the first byte is stored
   
///////////////////////////////////////////////////////////////////////////////   
   
   // ## Declaraing & Initializing Pointers ## //
   
   // Syantax: 
   //       data_type *pointer_name
   // Here the data_type refers to data type of the value that the pointer will point to (like int, char, etc)
   
   // Example:
   // int *ptr <--- points to interger value
   // char *ptr <--- points to character value
   // float *ptr <--- points to float value

  return 0;
}
